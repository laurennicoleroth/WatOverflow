<h3>Comments</h3>
<% @comments.each do |comment| %>
  <div>
    <strong>Commentable ID: <%= comment.commentable_id %></strong>
    <br />
    <p><%= comment.body %></p>
    <p><%= link_to 'Delete', comment_path(comment), method: :delete, data: { confirm: 'Are you sure?' } %></p>
  </div>
<% end %>
